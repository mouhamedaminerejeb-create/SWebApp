services:
  mongodb:
    image: mongo:latest
    ports:
      - 27017:27017
    expose:
      - 27017
    environment:
      MONGO_INITDB_DATABASE: sport_app_db
    networks:
      - apinet
    volumes:
      - mongodb_data:/data/db

  apirest:
    build: .
    ports:
      - 0.0.0.0:8888:8888
    depends_on:
      - mongodb
    environment:
      MONGO_HOST: mongodb
      MONGO_PORT: 27017
      MONGO_DB: sport_app_db
      MONGO_DB_COLLECTION1: teams
      MONGO_DB_COLLECTION2: matches
    networks:
      - apinet

networks:
  apinet:
    driver: bridge

volumes:
  mongodb_data:

